import { PagePlaceholder } from '@/components/ui/page-placeholder'

/** Lịch sử bật/tắt — placeholder until phase 05. */
export function OnoffHistoryPage() {
  return (
    <PagePlaceholder
      title="Lịch sử bật/tắt"
      subtitle="Theo dõi các lần thay đổi trạng thái của đèn LED."
    />
  )
}
