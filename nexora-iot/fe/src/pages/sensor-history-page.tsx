import { PagePlaceholder } from '@/components/ui/page-placeholder'

/** Lịch sử cảm biến — placeholder until phase 05. */
export function SensorHistoryPage() {
  return (
    <PagePlaceholder
      title="Lịch sử cảm biến"
      subtitle="Tra cứu dữ liệu cảm biến theo thời gian."
    />
  )
}
